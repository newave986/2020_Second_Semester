package r;

public class B extends q.A {
    void set() {
        // pri = 1;
        // private type은 사용 가능
        // def = 2;
        // default type은 같은 패키지 내에서 사용 가능
        pro = 3;
        pub = 4;
    }
}
